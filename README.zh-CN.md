# sculptor

> [Claude Code](https://claude.com/claude-code) 上下文管理工具。
> 把 session jsonl 翻成可编辑的 markdown → 让 agent 自由编辑(删段 = hide、改 body = merge、不动 = keep)→ 翻回新 jsonl 让 `claude --resume` 接着干。

把本仓库放到 `~/.claude/skills/sculptor/` 下。在 Claude Code 里说 `/sculptor` 调用,或者直接跑 `s1.py` / `s2.py`。

```bash
~/.claude/skills/sculptor/scripts/s1.py <session.jsonl>   # 翻成 edit.md
# agent 直接 read/edit edit.md
~/.claude/skills/sculptor/scripts/s2.py <session>.edit.md # 翻回新 jsonl
claude --resume <new-sid>                                  # 接着干
```

原 jsonl 永远不动。新 jsonl 落在原 jsonl 同目录,`claude --resume` 自动扫到。

---

## 跟 Sculptor 论文的关系

本仓库是我们 ICLR 2026 论文的 **skill 级实现 + 后续延伸**:

> **Sculptor: Empowering LLMs with Cognitive Agency via Active Context Management.**
> Mo Li, L.H. Xu, Qitai Tan, Long Ma, Ting Cao, Yunxin Liu, Flood Sung. ICLR 2026.
> [arXiv:2508.04664](https://arxiv.org/abs/2508.04664)

核心想法一致 —— 把"留什么、删什么"的决策权显式交给 agent,而不是依赖不透明的 auto-compaction。论文提出了三族 cognitive tools(fragmentation / summary-hide-restore / precise search),本仓库以最直接的形式实现了 **summary-hide-restore**:agent 读 markdown、编辑 markdown、我们再 re-encode。

```bibtex
@article{li2025sculptor,
  title   = {Sculptor: Empowering {LLMs} with Cognitive Agency via Active Context Management},
  author  = {Li, Mo and Xu, L.H. and Tan, Qitai and Ma, Long and Cao, Ting and Liu, Yunxin},
  journal = {arXiv preprint arXiv:2508.04664},
  year    = {2025},
  url     = {https://arxiv.org/abs/2508.04664}
}
```

---

## 安装

```bash
git clone https://github.com/Mor-Li/sculptor.git ~/.claude/skills/sculptor
pip install tiktoken
```

---

## 中间 markdown 长什么样

每条 record 一段,heading 形如 `### turn N · kind · b00NN · Nt · meta`:

```markdown
### turn 1 · user · b0001 · 925t
<user 原话>

### turn 1 · think · b0002 · 4340t
<thinking 内容,可能是空 + signature 说明>

### turn 1 · asst · b0003 · 1022t
<assistant 文本>

### turn 3 · call+result · b0042+b0043 · 66+58t · Bash
**call** (b0042):
description: ...
$ <command>

**result** (b0043):
<output>
```

`b00NN` 是 anchor,后处理用它定位原 jsonl 的 record。`tool_use` 跟对应的 `tool_result` 打包成同一段,删一起删避免配对孤儿。

## 三种意图

| agent 做的事 | 后处理结果 |
|---|---|
| 删整段 | hide(对应 record 被 stub/drop) |
| 改 body 内容 | merge replacement(替换成 synthetic record) |
| 不动 | keep 原样 |

只有一条约束:**不要改 heading 里的 `b00NN` id**。

## 哪些段该删 / 该改

参考真实 session 实测有效的 pattern(按出现频率):

- **大段 tool_result 转述**:agent 已经在下一条 think/asst 里转述了核心结论,verbatim 内容可删
- **同文件多次 Read / Edit 循环**:留最后一次 Read(反映当前 disk 状态),中间过期快照删掉
- **boilerplate 回执**:`Task #N created successfully` / `The file X has been updated successfully` / `Updated task #N status` 这种固定模板,内容零信息
- **失败 → 重试链**:把"失败→诊断→重试→成功"改写成一句"agent 意识到 X 因 Y 失败,改用 Z 成功"
- **整个失败实验段**:已被后续转述、不再被引用的探索 turn,整段直接删除或保留可能对未来有用的 summary 即可
- **SendMessage echo**:multi-agent session 里 SendMessage 的 result 通常是 call.content 的完整回声,删 result 保 call
- **超大单 result**:agent 拉了整个飞书文档 / git log / 数据集 dump,但只用了头尾几行 → 整段删,disk/远端有原件
- **大 image attachment(disk 有原图)**:agent 生成 / 截屏的 PNG 进入 session 后被 base64 嵌入,每张几 MB。判定条件:对应 `<file_path>` 在 disk 上还在(`output/*.png` / `screenshots/*.png` 之类)→ image content 整段删,保留 file_path 引用。原图 disk 上,resume 后 agent 想看图直接 Read 即可。**重要**:这还能解决 API 的 "image dimension > 2000px" 和 "many-image request" 限制 —— 长期运行 session 攒了几十张大 PNG 后会无法 resume,删掉冗余 image 是唯一出路
- **long-context 测试里明显不相关的段(谨慎使用)** ⚠️:针对 needle-in-haystack 类 benchmark —— user 输入是一大段 context + 一个具体问题,大部分 context 跟问题不相关。可以删**明显不相关**的部分(disclaimer / 章节标题 / 重复 boilerplate)。但**绝对要 conservative**:针(关键信息)可能藏在"看似不相关"的位置,**一次只删一小段,删完观察 resume 后回答是否还正确**;不要批量 hide 大段否则可能把针一起删了

口诀:**删了之后 server 端 resume 还能继续干同一件事,而且理论上删除的信息对做好这件事已经无明显收益吗?能 → 删**。

## ⚠️ 反模式:不要批量删 thinking signature

jsonl 里的 `thinking` 块看着可能是空(`thinking: ""`),但 `signature` 字段是 **server 能解码的加密完整 thinking 内容**,用于 round-trip 推理。删了 thinking 段会让推理质量在 resume 时 silently 下降。

事实(Anthropic 官方文档):

> "The signature field is **not just a verification hash**—it contains the encrypted full thinking content that the server can decode."
>
> "The server **decrypts the signature** to reconstruct the original thinking for prompt construction."

实测把删过空 thinking 的 jsonl `claude --resume`,API 不拒绝,但 Anthropic 文档明说 "the entire sequence of consecutive thinking blocks must match the outputs generated by the model during the original request",**推理质量可能 silently 下降**。如果删的是 tool-use 紧邻 turn 内的 thinking,API 可能直接 reject。

**不要做**:不要删空 thinking,不要删非空 thinking,thinking 段就让它在 markdown 里出现,agent 不动它。

## 边界 & 保证

- **原 jsonl 永远不动**:s2 写新文件到原目录,带 `<new-sid>.edit-manifest.json` 审计追溯
- **tool_use ↔ tool_result 配对**:s2 自动 stub / 补全保证 API 合法,agent 不用担心
- **parentUuid 链**:删 record 后自动缝合到最近存活祖先
- **merged synthetic record**:agent 改写的段会以 `model: "sculptor-synthetic"` 标记,正文带 "agent rewrote ..." 前缀,事后可审计
- **compact_boundary 警告** ⚠️:被 `/compact` 过的 session 含 `isCompactSummary: true` 的 user record,这是 compact 边界,不要删

## 依赖

- Python 3.10+
- `tiktoken`(cl100k_base 真实 token 计数)

---

## 相关文档

- [Sculptor 论文 (arXiv)](https://arxiv.org/abs/2508.04664)
- [Claude Code Skills 文档](https://docs.claude.com/en/docs/claude-code/skills)
- [SKILL.md](./SKILL.md) — 英文完整工作流和 pattern 指南
- [docs/jsonl-anatomy.md](./docs/jsonl-anatomy.md) — Claude Code session jsonl 格式深度剖析

---

## 许可

MIT — 见 [LICENSE](./LICENSE)。
